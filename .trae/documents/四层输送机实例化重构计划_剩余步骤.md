# 四层输送机实例化重构计划 - 剩余步骤实施（Step 4-5）

## 当前进度总览

### ✅ 已完成的步骤（Step 1-3）

| 步骤 | 文件 | 状态 | 说明 |
|------|------|------|------|
| **Step 1** | `四层输送机/FB_单层输送机_V4.0.0.st` | ✅ 完成 | 单层纯逻辑功能块（24入/10出，9步状态机） |
| **Step 2** | `四层输送机/FB_四层输送机_V4.0.0.st` | ✅ 完成 | 容器功能块（内部4个单层实例） |
| **Step 3** | `四层输送机/FBD使用说明.md` | ✅ 完成 | ASCII art FBD图（单层+四层容器） |

### ⏳ 待执行的步骤（Step 4-5）

| 步骤 | 文件 | 状态 | 说明 |
|------|------|------|------|
| **Step 4** | `主控/主控程序_V4.0.0.st` | ⏳ 待执行 | 适配新容器FB接口（参数名调整+补全输出） |
| **Step 5** | `PLC变量定义文档_VAR-DJ-2026-005-V4.0.0.md` | ⏳ 待执行 | 新增单层FB定义+更新容器FB说明 |

---

## Step 4: 适配主控程序（核心变更）

### 4.1 变更目标文件

**文件路径**: `02_PLC程序/通用ST程序及变量表/主控/主控程序_V4.0.0.st`

**变更范围**: 第488-543行（fb四层输送机调用部分）

### 4.2 接口差异分析

#### 当前主控调用方式（旧接口）
```st
fb四层输送机(
    (* 输入 *)
    i_b使能               := b系统使能,
    i_b自动模式           := bHMI_自动模式,
    ...
    i_bLx阻挡下降         := bLx_阻挡下降,      // ❌ 旧名称: 带"Lx"前缀
    i_bLx阻挡上升         := bLx_阻挡上升,
    ...
    i_Lx_分料前感应器     := bLx_分料前感应器,  // ❌ 旧名称: 带"Lx_"前缀
    ...

    (* 输出 *)
    o_Lx_阻挡电磁阀       => bLx_阻挡电磁阀,    // ❌ 旧名称: 带"Lx_"前缀
    o_Lx_分料电磁阀       => bLx_分料电磁阀,
    ...
    o_Lx_放料完成         =>                     // ❌ 语法错误: 未正确接收数组
    (
        b输送机_L1_放料完成 := o_Lx_放料完成[1],  // ❌ 无效语法
        ...
    ),
    o_b运行中             => bHMI_输送机运行中,
    o_b故障               => ,                    // ⚠️ 未连接
    o_i当前状态           => ,                    // ⚠️ 未连接
    o_Lx_当前步序          => ,                    // ⚠️ 未连接
    o_i本站报警代码        =>                      // ⚠️ 未连接
);
```

#### 新容器FB期望的调用方式（新接口）
```st
fb四层输送机(
    (* 输入 - 参数名已调整 *)
    i_b使能               := b系统使能,
    i_b自动模式           := bHMI_自动模式,
    ...
    i_b阻挡下降           := bLx_阻挡下降,      // ✅ 新名称: 去掉"Lx"前缀
    i_b阻挡上升           := bLx_阻挡上升,
    ...
    i_分料前感应器         := bLx_分料前感应器,  // ✅ 新名称: 去掉"Lx_"前缀
    i_b取放料完成          :=                    // ✅ 新增: 下游反馈输入
    (
        b输送机_L1_放料完成,  // L1取放料完成
        b输送机_L2_放料完成,  // L2取放料完成
        b输送机_L3_放料完成,  // L3取放料完成
        b输送机_L4_放料完成   // L4取放料完成
    ),

    (* 输出 - 全部正确连接 *)
    o_阻挡电磁阀           => bLx_阻挡电磁阀,    // ✅ 新名称
    o_分料电磁阀           => bLx_分料电磁阀,
    o_输送带正转           => bLx_输送带正转,
    o_输送带慢速           => bLx_输送带慢速,
    o_输送带反转           => bLx_输送反转,      // ✅ 补全: 原来未连接
    o_b放料完成            =>
    (
        b输送机_L1_放料完成,  // ✅ 正确数组输出语法
        b输送机_L2_放料完成,
        b输送机_L3_放料完成,
        b输送机_L4_放料完成
    ),
    o_b运行中             => bHMI_输送机运行中,
    o_b故障               => ,                    // 可选: 连接到全局故障汇总
    o_i当前状态           => ,                    // 可选: HMI显示用
    o_Lx当前步序           => ,                    // 可选: 每层独立步序显示
    o_i本站报警代码        =>                      // ✅ 应连接到公共报警FB
);
```

### 4.3 具体修改清单

#### 修改点 1: 输入参数名称调整（6个手动操作）

| 旧参数名（主控中使用） | 新参数名（容器FB定义） | 修改位置 |
|------------------------|------------------------|----------|
| `i_bLx阻挡下降` | `i_b阻挡下降` | 第498行 |
| `i_bLx阻挡上升` | `i_b阻挡上升` | 第499行 |
| `i_bLx分料推出` | `i_b分料推出` | 第500行 |
| `i_bLx分料复位` | `i_b分料复位` | 第501行 |
| `i_bLx输送正转` | `i_b输送正转` | 第502行 |
| `i_bLx输送反转` | `i_b输送反转` | 第503行 |

#### 修改点 2: 输入参数名称调整（7个传感器）

| 旧参数名（主控中使用） | 新参数名（容器FB定义） | 修改位置 |
|------------------------|------------------------|----------|
| `i_Lx_分料前感应器` | `i_分料前感应器` | 第511行 |
| `i_Lx_到位感应器1` | `i_到位感应器1` | 第512行 |
| `i_Lx_到位感应器2` | `i_到位感应器2` | 第513行 |
| `i_Lx_阻挡气缸上位` | `i_阻挡气缸上位` | 第514行 |
| `i_Lx_阻挡气缸下位` | `i_阻挡气缸下位` | 第515行 |
| `i_Lx_分料气缸上位` | `i_分料气缸上位` | 第516行 |
| `i_Lx_分料气缸下位` | `i_分料气缸下位` | 第517行 |

#### 修改点 3: 新增下游反馈输入（1个数组）

**新增参数**: `i_b取放料完成 : ARRAY[1..4] OF BOOL`

**插入位置**: 在传感器输入之后（第517行之后）

**数据来源**: 取放料机构的反馈信号（目前为占位符，后续联调时连接）

**初始值建议**:
```st
i_b取放料完成          :=
(
    FALSE,  // L1: 占位符（待联调时连接取放料机构反馈）
    FALSE,  // L2: 占位符
    FALSE,  // L3: 占位符
    FALSE   // L4: 占位符
),
```

或者连接到中间变量：
```st
i_b取放料完成          :=
(
    b输送机_L1_允许放料,  // 来自取放料机构的反馈
    b输送机_L2_允许放料,
    b输送机_L3_允许放料,
    b输送机_L4_允许放料
),
```

#### 修改点 4: 输出参数名称调整（5个执行器）

| 旧参数名（主控中使用） | 新参数名（容器FB定义） | 修改位置 |
|------------------------|------------------------|----------|
| `o_Lx_阻挡电磁阀` | `o_阻挡电磁阀` | 第520行 |
| `o_Lx_分料电磁阀` | `o_分料电磁阀` | 第521行 |
| `o_Lx_输送带正转` | `o_输送带正转` | 第522行 |
| `o_Lx_输送带慢速` | `o_输送带慢速` | 第523行 |

**新增输出连接**:
- `o_输送带反转 => bLx_输送反转` （第524行，原来未连接）

#### 修改点 5: 修正下游信号输出语法（关键修复）

**旧代码（第527-533行）- 语法错误**:
```st
o_Lx_放料完成         =>
(
    b输送机_L1_放料完成 := o_Lx_放料完成[1],  // ❌ 无效语法
    b输送机_L2_放料完成 := o_Lx_放料完成[2],
    b输送机_L3_放料完成 := o_Lx_放料完成[3],
    b输送机_L4_放料完成 := o_Lx_放料完成[4]
),
```

**新代码（修正后）**:
```st
o_b放料完成            =>
(
    b输送机_L1_放料完成,  // ✅ 正确: 数组元素按顺序赋值
    b输送机_L2_放料完成,
    b输送机_L3_放料完成,
    b输送机_L4_放料完成
),
```

#### 修改点 6: 补全未连接的输出（可选但推荐）

| 输出参数 | 建议连接目标 | 说明 |
|----------|--------------|------|
| `o_b故障` | `bHMI_任何报警激活` 或临时变量 | 故障标志汇总 |
| `o_i当前状态` | `iHMI_四层当前状态` (新增D区变量) | HMI显示主要状态码 |
| `o_Lx当前步序` | `giL1当前步序 ~ giL4当前步序` (4个INT变量) | 每层独立步序显示 |
| `o_i本站报警代码` | 连接到 `fb公共报警.i_i输送机报警` | **必须连接**：公共报警管理器需要 |

**特别注意**: `o_i本站报警代码` 必须连接到公共报警FB，否则报警系统无法工作！

#### 修改点 7: 更新注释说明

在 fb四层输送机 调用处添加注释块：
```st
(*==============================================================================
  Step4: 调用fb四层输送机 (工站1: 分料/输送控制)
  架构: V4.0.0 容器模式 - 内部实例化4个 FB_单层输送机
  接口: 外部保持ARRAY[1..4]形式，内部自动分发到各单层实例
  ==============================================================================*)
```

### 4.4 预期修改后的完整调用代码（参考模板）

```st
(*==============================================================================
  Step4: 调用fb四层输送机 (工站1: 分料/输送控制)
  说明: 传入所有传感器输入+HMI控制信号+工艺参数+下游反馈
        接收所有执行器请求输出+下游信号+状态+报警
  架构: V4.0.0 容器模式 (4× FB_单层输送机 实例)
  ==============================================================================*)

fb四层输送机(
    (* 一、系统控制信号 (公共,透传给所有层) *)
    i_b使能               := b系统使能,
    i_b自动模式           := bHMI_自动模式,
    i_b手动模式           := bHMI_手动模式,
    i_b启动               := bHMI_启动,
    i_b停止               := bHMI_停止,
    i_b复位               := bHMI_复位 OR b复位按钮,

    (* 二、手动操作信号 (每层6个, 共24个) *)
    i_b阻挡下降           := bLx_阻挡下降,
    i_b阻挡上升           := bLx_阻挡上升,
    i_b分料推出           := bLx_分料推出,
    i_b分料复位           := bLx_分料复位,
    i_b输送正转           := bLx_输送正转,
    i_b输送反转           := bLx_输送反转,

    (* 三、工艺参数 (公共) *)
    i_r输送速度           := rHMI_输送速度设定,
    i_i分料时间           := iHMI_分料时间设定,
    i_i阻挡等待时间        := iHMI_升降动作时间设定,

    (* 四、传感器输入 (4层×7类=28个) *)
    i_分料前感应器         := bLx_分料前感应器,
    i_到位感应器1          := bLx_到位感应器1,
    i_到位感应器2          := bLx_到位感应器2,
    i_阻挡气缸上位         := bLx_阻挡气缸上位,
    i_阻挡气缸下位         := bLx_阻挡气缸下位,
    i_分料气缸上位         := bLx_分料气缸上位,
    i_分料气缸下位         := bLx_分料气缸下位,

    (* 五、下游反馈信号 (取放料机构已取走该层物料) *)
    i_b取放料完成          :=
    (
        b输送机_L1_放料完成,  (* L1: 占位符或连接实际反馈 *)
        b输送机_L2_放料完成,  (* L2: 占位符或连接实际反馈 *)
        b输送机_L3_放料完成,  (* L3: 占位符或连接实际反馈 *)
        b输送机_L4_放料完成   (* L4: 占位符或连接实际反馈 *)
    ),

    (* 六、执行器请求输出 (4层×5=20个) -> 映射到物理Y地址 *)
    o_阻挡电磁阀           => bLx_阻挡电磁阀,
    o_分料电磁阀           => bLx_分料电磁阀,
    o_输送带正转           => bLx_输送带正转,
    o_输送带慢速           => bLx_输送带慢速,
    o_输送带反转           => bLx_输送反转,

    (* 七、下游信号输出 (给取放料机构) *)
    o_b放料完成            =>
    (
        b输送机_L1_放料完成,
        b输送机_L2_放料完成,
        b输送机_L3_放料完成,
        b输送机_L4_放料完成
    ),

    (* 八、状态/HMI显示输出 *)
    o_b运行中             => bHMI_输送机运行中,
    o_b故障               => ,  (* 可选: 连接到全局故障汇总 *)
    o_i当前状态           => ,  (* 可选: HMI显示用 *)
    o_Lx当前步序           => ,  (* 可选: 每层独立步序显示 *)

    (* 九、报警输出 (给公共报警管理器) *** 必须连接 ***) *)
    o_i本站报警代码        =>   (* *** 重要: 此处需连接到fb公共报警 *** *)
);
```

---

## Step 5: 更新PLC变量定义文档

### 5.1 变更目标文件

**文件路径**: `02_PLC程序/通用ST程序及变量表/PLC变量定义文档_VAR-DJ-2026-005-V4.0.0.md`

### 5.2 具体修改内容

#### 修改点 1: 新增 FB_单层输送机 接口定义章节

在文档中"功能块接口定义"部分新增一个子章节：

```markdown
### 3.x FB_单层输送机 (单层输送机纯逻辑功能块)

**文件位置**: `02_PLC程序/通用ST程序及变量表/四层输送机/FB_单层输送机_V4.0.0.st`
**版本**: V4.0.0
**类型**: FUNCTION_BLOCK (基础单元)
**实例化次数**: 4次 (由 FB_四层输送机 容器内部实例化)

#### 3.x.1 VAR_INPUT (24个输入)

| 序号 | 变量名 | 数据类型 | 功能描述 | 来源 |
|------|--------|----------|----------|------|
| 1 | `i_b使能` | BOOL | 总使能信号 | 主控系统就绪标志 |
| 2 | `i_b自动模式` | BOOL | 自动运行模式选择 | HMI M区变量 |
| ... | ... | ... | ... | ... |
| 23 | `i_分料气缸下位` | BOOL | 分料气缸推出到位 | DI模块 |
| 24 | `i_b上游允许放料` | BOOL | 上游允许继续放料 | 取放料机构反馈 |

#### 3.x.2 VAR_OUTPUT (10个输出)

| 序号 | 变量名 | 数据类型 | 功能描述 | 目标 |
|------|--------|----------|----------|------|
| 1 | `o_阻挡电磁阀` | BOOL | 阻挡气缸控制 | → 主控 → Y地址 |
| ... | ... | ... | ... | ... |
| 10 | `o_i报警代码` | INT | 报警代码(0~7) | → 公共报警管理器 |

#### 3.x.3 内部常量 (VAR_CONSTANT)

| 常量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `T_DEF_阻挡动作时间` | INT | 800ms | 阻挡气缸动作超时 |
| ... | ... | ... | ... |
```

#### 修改点 2: 更新 FB_四层输送机 接口定义章节

在现有章节中添加说明：

```markdown
### 3.y FB_四层输送机 (四层输送机容器功能块)

**文件位置**: `02_PLC程序/通用ST程序及变量表/四层输送机/FB_四层输送机_V4.0.0.st`
**版本**: V4.0.0 (重构为容器模式)
**类型**: FUNCTION_BLOCK (容器)
**架构变化**: 从V3.x.x的巨型FB(内含4层数组逻辑) 重构为V4.0.0的容器FB(内含4个单层实例)

**V4.0.0架构说明**:
- 内部声明4个 `FB_单层输送机` 实例 (fbLayer1~4)
- 职责: 输入分发(ARRAY→单个) → 调用实例 → 输出收集(单个→ARRAY)
- 不包含业务逻辑，仅做信号路由和数据聚合
- 外部接口保持ARRAY[1..4]形式，与主控程序完全兼容

**接口兼容性**: ✅ 与V3.x.x版本的外部接口完全兼容（参数名已统一调整）
```

#### 修改点 3: 更新全局变量表（如需新增变量）

如果在Step 4中决定连接某些可选输出，可能需要在VAR区新增以下变量：

```st
(*--- 四层输送机状态显示 (可选) ---*)
gi四层当前状态        : INT;     (* o_i当前状态 -> HMI显示 *)
giL1当前步序          : INT;     (* o_Lx当前步序[1] -> HMI显示 *)
giL2当前步序          : INT;     (* o_Lx当前步序[2] -> HMI显示 *)
giL3当前步序          : INT;     (* o_Lx当前步序[3] -> HMI显示 *)
giL4当前步序          : INT;     (* o_Lx当前步序[4] -> HMI显示 *)

(*--- 四层输送机下游反馈 (用于循环控制) ---*)
b输送机_L1_允许放料  : BOOL;    (* 取放料机构反馈 -> i_b取放料完成[1] *)
b输送机_L2_允许放料  : BOOL;    (* 取放料机构反馈 -> i_b取放料完成[2] *)
b输送机_L3_允许放料  : BOOL;    (* 取放料机构反馈 -> i_b取放料完成[3] *)
b输送机_L4_允许放料  : BOOL;    (* 取放料机构反馈 -> i_b取放料完成[4] *)
```

---

## 执行顺序与依赖关系

```
Step 4: 适配主控程序
    │
    ├── 4.1 修改输入参数名 (6个手动 + 7个传感器)
    │
    ├── 4.2 新增下游反馈输入 (i_b取放料完成)
    │
    ├── 4.3 修改输出参数名 (5个执行器 + 1个下游信号)
    │
    ├── 4.4 补全未连接的输出 (输送反转 + 故障 + 步序 + 报警码)
    │
    └── 4.5 更新注释说明
         │
         ▼
Step 5: 更新变量定义文档
    │
    ├── 5.1 新增 FB_单层输送机 接口定义章节
    │
    ├── 5.2 更新 FB_四层输送机 说明（标注容器架构）
    │
    └── 5.3 更新全局变量表（如需新增变量）
```

**注意**: Step 5 的内容依赖于 Step 4 的最终修改结果（特别是是否新增了中间变量）

---

## 风险评估与应对措施

| 风险项 | 影响程度 | 发生概率 | 应对措施 |
|--------|----------|----------|----------|
| 参数名修改遗漏导致编译错误 | 高 | 低 | 逐一对照容器FB的VAR_INPUT/VAR_OUTPUT定义检查 |
| 输出连接语法错误（特别是数组输出） | 高 | 中 | 使用上述参考模板，严格遵循IEC 61131-3语法 |
| o_i本站报警代码未连接导致报警失效 | 高 | 中 | **重点检查项**，必须在Step 4中正确连接 |
| 下游反馈信号(i_b取放料完成)暂无真实来源 | 低 | 高 | 使用FALSE占位符或临时变量，后续联调时替换 |

---

## 验证标准

### Step 4 完成标准

- [ ] 所有输入参数名与容器FB定义完全一致（无编译错误）
- [ ] 所有输出参数名与容器FB定义完全一致
- [ ] `o_输送带反转` 已正确连接（不再悬空）
- [ ] `o_b放料完成` 使用正确的数组赋值语法
- [ ] `o_i本站报警代码` 已连接到公共报警FB或中间变量
- [ ] 注释已更新标注V4.0.0容器架构

### Step 5 完成标准

- [ ] 文档中包含完整的 `FB_单层输送机` 接口定义（24入/10出）
- [ ] `FB_四层输送机` 说明已更新为容器模式
- [ ] 如有新增全局变量，已在文档中记录
- [ ] 版本号已更新为V4.0.0（如适用）

---

## 后续工作（本次计划范围外）

完成Step 4-5后，建议进行以下后续工作：

1. **编译验证**: 在GX Works3或对应IDE中编译整个项目，确认无错误
2. **仿真测试**: 使用PLC仿真软件验证4层独立运行逻辑
3. **接口联调**: 将 `i_b取放料完成` 连接到真实的取放料机构反馈信号
4. **HMI画面更新**: 根据新增的状态显示变量(o_Lx当前步序等)更新HMI画面
5. **程序设计总文档更新**: 同步更新 `016_DJ-2026-005_PLC程序设计总文档_PLC-V4.0.0.md`

---

**编制日期**: 2026-04-23
**编制人**: AI技术助手
**状态**: 待用户审核确认后执行
