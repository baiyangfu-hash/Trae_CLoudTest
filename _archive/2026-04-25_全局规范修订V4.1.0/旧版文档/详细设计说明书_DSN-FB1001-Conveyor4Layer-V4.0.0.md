# FB_1001_Conveyor4Layer & FB_1002_SingleLayerConveyor 详细设计说明书

## 1. 文档基础信息

| 属性 | 值 |
|------|-----|
| **文档标题** | 四层输送机功能块详细设计说明书（含单层+容器两级架构） |
| **适用FB** | FB_1001_Conveyor4Layer_BufferFraming (容器) + FB_1002_SingleLayerConveyor_BufferFraming (单层) |
| **文档版本** | V4.0.0 |
| **编制日期** | 2026-04-24 |
| **编制人** | Trae |
| **审核人** | [待审核] |
| **遵循规范** | 810_PLC编程规范_DEV-V1.0.0, 801_PLC变量命名与功能块命名规范_DEV-V1.0.3 |

## 2. 变更记录摘要

> 完整变更历史请参阅: [变更记录_CHG-FB1001-Conveyor4Layer-V4.0.0.md](./变更记录_CHG-FB1001-Conveyor4Layer-V4.0.0.md)

| 版本号 | 日期 | 变更内容摘要 | 详细说明链接 |
|--------|------|-------------|-------------|
| V4.0.0 | 2026-04-24 | 文档体系建立，从FBD使用说明拆分为独立DSN文档 | [→ V4.0.0变更详情](./变更记录_CHG-FB1001-Conveyor4Layer-V4.0.0.md#v400) |

---

## 3. 功能描述

### 3.1 核心功能概述

四层输送机系统采用**两级功能块层次结构**设计：

#### 容器功能块 - FB_1001_Conveyor4Layer
- **职责**: 纯容器，负责输入分发和输出收集
- **不包含业务逻辑**: 仅将主控传入的数组信号拆分给4个单层FB实例
- **接口统一**: 对外提供统一的ARRAY[1..4]接口，隐藏内部层次结构

#### 单层功能块 - FB_1002_SingleLayerConveyor
- **职责**: 单层输送带的完整控制逻辑
- **9步自动状态机**: 空闲→等待启动→推料气缸伸出→输送带正转→检测到位→输送带停止→推料气缸缩回→等待循环结束→返回空闲
- **完全解耦**: 无任何X/Y/M/D物理地址引用，所有数据通过逻辑变量交互
- **内置报警机制**: 5种报警类型的检测、锁存与输出（101~199段）

### 3.2 工艺流程概述

```
四层输送机工艺流程(以单层为例):

┌─────────────────────────────────────────────────────────────┐
│  核心工艺: 接收上游边框 → 输送到取料位 → 通知下游取放料     │
│                                                             │
│  自动循环流程:                                              │
│  ┌─── 步骤0: 空闲/待机位置                                  │
│  │    └── 等待启动触发                                      │
│  │                                                          │
│  ├─── 步骤1: 等待放料完成                                   │
│  │    └── 等待 i_Lx_放料完成 = TRUE                        │
│  │                                                          │
│  ├─── 步骤2: 推料气缸伸出                                   │
│  │    └── o_Lx_推料气缸伸出 := TRUE                        │
│  │                                                          │
│  ├─── 步骤3: 输送带正转                                    │
│  │    ├── o_Lx_输送带正转 := TRUE                          │
│  │    └── 同时保持推料气缸伸出                              │
│  │                                                          │
│  ├─── 步骤4: 检测产品到位                                   │
│  │    └── 等待 i_Lx_产品到位 = TRUE                        │
│  │                                                          │
│  ├─── 步骤5: 输送带停止                                    │
│  │    └── o_Lx_输送带正转 := FALSE                         │
│  │                                                          │
│  ├─── 步骤6: 推料气缸缩回                                   │
│  │    └── o_Lx_推料气缸缩回 := TRUE                        │
│  │                                                          │
│  ├─── 步骤7: 等待循环结束条件                               │
│  │    └── 确认气缸已缩回且无故障                           │
│  │                                                          │
│  └─── 步骤8: 发送放料完成 → 返回步骤1                       │
│       └── o_Lx_放料完成 := TRUE (脉冲)                     │
│                                                             │
│  循环特点: 步骤8完成后自动回到步骤1, 无需重新启动           │
│  4层独立性: 每层有独立的FB实例, 互不干扰                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 架构定位与层次结构

### 4.1 两级功能块层次结构图

```
系统架构总览:

┌─────────────────────────────────────────────────────────────────────┐
│                           主控程序 (Main / PRG_Main)                 │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │              FB_1001_Conveyor4Layer (容器功能块)               │   │
│   │                职责: 输入分发 + 输出收集                      │   │
│   │                                                                     │   │
│   │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐│   │
│   │  │fbLayer1      │ │fbLayer2      │ │fbLayer3      │ │fbLayer4      ││   │
│   │  │FB_1002_SingleLayerConveyor  │ │FB_1002_SingleLayerConveyor  │ │FB_1002_SingleLayerConveyor  ││   │
│   │  │  (第1层/底层) │ │  (第2层)     │ │  (第3层)     │ │  (第4层/顶层) ││   │
│   │  │  9步状态机    │ │  9步状态机    │ │  9步状态机    │ │  9步状态机    ││   │
│   │  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘│   │
│   └─────────────────────────────────────────────────────────────┘   │
│         ▲                        ▲                        ▲               │
│         │ 输入分发(ARRAY->单个)   │                        │ 输出收集       │
│         └────────────────────────┴────────────────────────┘ (单个->ARRAY)  │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 职责分工表

| 维度 | FB_1001 (容器) | FB_1002 (单层) |
|:-----|:---------------|:--------------|
| **主要职责** | 信号路由和聚合 | 业务逻辑控制 |
| **是否包含状态机** | ❌ 不包含 | ✅ 包含9步状态机 |
| **输入处理** | 将ARRAY[1..4]拆分为单个信号 | 接收单个层的信号 |
| **输出处理** | 将单个输出聚合成ARRAY[1..4] | 输出单个层的控制信号 |
| **报警处理** | 收集4层报警并输出最高优先级 | 检测本层报警并输出代码 |
| **实例数量** | 1个 | 4个(Layer1~Layer4) |
| **复杂度** | 低（纯数据映射） | 中（完整状态机） |

### 4.3 数据流向示意

```
数据流向详解:

主控程序(Main)
    │
    ├─ 输入(ARRAY[1..4])
    │   ├─ i_bLx_启动[1..4]
    │   ├─ i_bLx_手动正转[1..4]
    │   ├─ i_bLx_手动反转[1..4]
    │   ├─ i_bLx_手动推料伸出[1..4]
    │   ├─ i_bLx_手动推料缩回[1..4]
    │   ├─ i_bLx_放料完成[1..4]
    │   └─ i_bLx_产品到位[1..4]
    │
    ▼
FB_1001 (容器)
    │
    ├─ 输入分发:
    │   ├─ fbLayer1.i_b启动 := i_bLx_启动[1]
    │   ├─ fbLayer2.i_b启动 := i_bLx_启动[2]
    │   ├─ fbLayer3.i_b启动 := i_bLx_启动[3]
    │   └─ fbLayer4.i_b启动 := i_bLx_启动[4]
    │   (... 其他信号同理 ...)
    │
    ├─ 调用4个FB_1002实例:
    │   ├─ fbLayer1()  ← 第1层(底层)
    │   ├─ fbLayer2()  ← 第2层
    │   ├─ fbLayer3()  ← 第3层
    │   └─ fbLayer4()  ← 第4层(顶层)
    │
    └─ 输出收集:
        ├─ q_bLx_输送带正转[1] := fbLayer1.o_输送带正转
        ├─ q_bLx_输送带正转[2] := fbLayer2.o_输送带正转
        ├─ q_bLx_输送带正转[3] := fbLayer3.o_输送带正转
        └─ q_bLx_输送带正转[4] := fbLayer4.o_输送带正转
        (... 其他输出同理 ...)
    
    ▼
主控程序(Main)
    └─ 输出映射到物理DO地址
```

### 4.4 设计原则

| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **层次化封装** | 容器屏蔽内部细节 | 主控只需与容器交互，无需知道内部有4个单层FB |
| **单一职责** | 容器只做路由，单层只做逻辑 | 高内聚低耦合 |
| **完全解耦** | 无任何X/Y/M/D引用 | 所有接口为VAR_INPUT/VAR_OUTPUT逻辑变量 |
| **4层独立运行** | 各层状态机互不干扰 | 4个独立FB实例，独立计数器和状态 |
| **默认值回退** | 未连接的参数使用安全默认值 | T_DEF_*系列常量提供回退值 |
| **故障安全** | 使能丢失时全部输出归零 | `i_b使能=FALSE` 强制清零 |

---

## 5. 状态机设计 (FB_1002 单层)

### 5.1 状态转换总图

```
单层输送机9步状态机:

                         ┌──────────────────────────────────────────────┐
                         │            启动触发(i_b启动)                  │
                         └─────────────────────┬────────────────────────┘
                                               │
                                               v
┌──────┐  ┌──────────────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐
│  0   │->│      1       │->│  2   │->│  3   │->│  4   │->│  5   │->│  6   │->│  7   │->│  8   │
│ 空闲 │  │等待放料完成  │  │推料  │  │输送  │  │检测  │  │输送  │  │推料  │  │等待  │  │发送  │
│      │  │              │  │伸出  │  │正转  │  │到位  │  │停止  │  │缩回  │  │循环  │  │完成  │
└──^───┘  └──────┬───────┘  └──┬---┘  └──┬---┘  └──┬---┘  └──┬---┘  └──┬---┘  └──┬---┘  └──┬---┘
   │             │             │         │         │         │         │         │         │
   │     ┌───────┴───────┐     │超时     │超时     │超时     │         │超时     │         │
   │     │ 放料完成       │     v         v         v         │         v         │         │
   │     │ (i_Lx_放料完成)│ ┌────────┐ ┌────────┐ ┌────────┐ │    ┌────────┐ │         │
   │     │               ││  104   │ │  102   │ │  103   │ │    │  105   │ │         │
   │     └───────────────┘│伸出超时│ │正转超时│ │到位超时│ │    │缩回超时│ │         │
   │                       └────────┘ └────────┘ └────────┘ │    └────────┘ │         │
   │                                                        │             │         │
   │                                                        │  (回到步骤1, 继续循环)          │
   └────────────────────────────────────────────────────────┴─────────────┴─────────┘
   
   循环特点: 步骤8完成后自动回到步骤1, 无需重新启动
   停止条件: 按下i_b停止按钮 -> 回到步骤0
```

### 5.2 各步骤详细说明表

| 步骤 | 名称 | 输入条件 | 动作输出 | 转出条件 | 转出目标 | 超时报警 | 典型时间 |
|:----:|:-----|:---------|:---------|:---------|:--------:|:-------:|:-------:|
| 0 | 空闲/待机位置 | - | 全部OFF | 启动触发(上升沿) | 步骤1 | - | - |
| 1 | 等待放料完成 | 进入步骤0 | 无 | 放料完成=TRUE | 步骤2 | - | 取决于上游 |
| 2 | 推料气缸伸出 | 放料完成 | 推料伸出=TRUE | 伸出到位(磁性开关) | 步骤3 | ALM_104 (5s) | ~0.5~1s |
| 3 | 输送带正转 | 推料伸出到位 | 正转=TRUE + 保持伸出 | 产品到位光电=TRUE | 步骤4 | ALM_102 (10s) | ~2~3s |
| 4 | 检测产品到位 | 输送中 | 保持当前输出 | 到位信号稳定(500ms去抖) | 步骤5 | ALM_103 (5s) | ~0.5s |
| 5 | 输送带停止 | 产品到位确认 | 正转=FALSE | 停止完成(延迟100ms) | 步骤6 | - | ~0.1s |
| 6 | 推料气缸缩回 | 输送停止 | 缩回=TRUE | 缩回到位(磁性开关) | 步骤7 | ALM_105 (5s) | ~0.5~1s |
| 7 | 等待循环结束 | 缩回到位 | 无额外动作 | 确认安全状态 | 步骤8 | - | 即时 |
| 8 | 发送放料完成 | 循环结束 | 放料完成=TRUE(脉冲) | 脉冲结束(200ms) | 步骤1 | - | 0.2s |

### 5.3 安全区管理

> 四层输送机的安全区管理与打胶机送料机构不同，它主要通过**上下游信号联动**实现安全：

```
上下游信号联动:

上游设备(来料方向):
  └─ i_Lx_放料完成[1..4]: 上游通知本层可以开始工作
      └─ 步骤1的转换条件
  
本设备(四层输送机):
  └─ o_Lx_放料完成[1..4]: 本层通知下游可以取料
      └─ 步骤8的输出(脉冲信号)
  
下游设备(取料方向): 取放料机构(FB_1003)
  └─ 接收 o_Lx_放料完成[1..4] 作为其输入
  
安全特点:
  ✅ 4层完全独立, 互不干涉
  ✅ 每层有自己的状态机和报警系统
  ✅ 通过脉冲信号(200ms)避免持续占用
  ✅ 无机械运动干涉风险(各层垂直堆叠)
```

---

## 6. 报警机制设计

### 6.1 报警码定义 (101~199段)

| 报警码 | 含义 | 触发条件 | 触发步骤 | 清除条件 | 优先级 |
|--------|------|----------|----------|----------|:------:|
| 101 | *(预留)* | - | - | - | - |
| 102 | 输送带正转超时 | 输送带未在规定时间(10s)内将产品送达 | 步骤3 | 复位按钮 | 高 |
| 103 | 产品到位检测超时 | 产品到位光电在规定时间(5s)内未检测到产品 | 步骤4 | 复位按钮 | 中 |
| 104 | 推料气缸伸出超时 | 推料气缸未在规定时间(5s)内伸出到位 | 步骤2 | 复位按钮 | 高 |
| 105 | 推料气缸缩回超时 | 推料气缸未在规定时间(5s)内缩回到位 | 步骤6 | 复位按钮 | 高 |

### 6.2 多层报警汇总机制

```
报警优先级汇总(FB_1001容器层):

4个单层FB各自产生报警代码(0或101~105):
  Layer1: o_i本站报警代码 = 0 (正常)
  Layer2: o_i本站报警代码 = 104 (伸出超时)
  Layer3: o_i本站报警代码 = 0 (正常)
  Layer4: o_i本站报警代码 = 0 (正常)

容器层汇总逻辑:
  o_i本站报警代码 := MIN(
      fbLayer1.o_i本站报警代码,
      fbLayer2.o_i本站报警代码,
      fbLayer3.o_i本站报警代码,
      fbLayer4.o_i本站报警代码
  );
  
  结果: o_i本站报警代码 := 104 (输出最小非零值)

原则: 数值越小优先级越高; 0表示无报警(不参与MIN比较)
```

---

## 7. 初始化流程

### 7.1 初始化触发条件

以下任一条件满足时触发初始化：
1. **首次使能**: `i_b使能` 从FALSE变为TRUE
2. **复位操作**: 操作员按下`i_b复位`按钮
3. **故障清除**: 报警被复位清除后

### 7.2 初始化执行流程

```
初始化流程(每个单层FB独立执行):

上电 / 使能首次有效 / 复位清除故障
                    │
                    v
          ┌─────────────────────┐
          │   开始初始化流程     │
          │  s_b初始化中 := TRUE │
          └──────────┬──────────┘
                     │
                     v
          ┌─────────────────────────────────────────────┐
          │  安全输出归零:                                │
          │    o_Lx_推料气缸伸出 := FALSE                 │
          │    o_Lx_推料气缸缩回 := FALSE                 │
          │    o_Lx_输送带正转 := FALSE                   │
          │    o_Lx_输送带反转 := FALSE                   │
          │    o_Lx_放料完成 := FALSE                     │
          │    s_i步序 := STP_空闲(0)                    │
          └──────────┬──────────────────────────────────┘
                     │
                     v
          ┌─────────────────────────────────────────────┐
          │  等待初始化条件满足:                          │
          │    条件A: 推料气缸在缩回位置                  │
          │           (i_Lx_推料缩回位 = TRUE)            │
          │    OR                                        │
          │    条件B: 初始化超时定时器到期(10秒)            │
          │           (超时强制完成，容错处理)              │
          └──────────┬──────────────────────────────────┘
                     │
                     v
          ┌─────────────────────┐
          │  s_b初始化完成:=TRUE │
          │  s_b初始化中 :=FALSE │
          │  进入正常工作循环     │
          └─────────────────────┘
```

**注意**: 
- 4个单层FB**并行独立初始化**，互不影响
- 容器FB(FB_1001)**不需要单独初始化**（纯数据路由，无状态）
- 初始化期间不执行自动/手动逻辑

---

## 8. 手动模式设计

### 8.1 手动操作功能

在手动模式下(`i_b手动模式=TRUE`)，每层提供4个手动操作点：

| 手动操作 | 功能描述 | 互锁关系 | 安全限制 |
|---------|---------|----------|----------|
| `i_bLx_手动正转` | 该层输送带正向点动 | 与手动反转互斥 | 受过载保护 |
| `i_bLx_手动反转` | 该层输送带反向点动 | 与手动正转互斥 | 受过载保护 |
| `i_bLx_手动推料伸出` | 该层推料气缸伸出点动 | 与手动推料缩回互斥 | 受限位保护 |
| `i_bLx_手动推料缩回` | 该层推料气缸缩回点动 | 与手动推料伸出互斥 | 受限位保护 |

### 8.2 数组索引约定

```
数组索引说明:

ARRAY[1] = L1 (第1层 / 底层)  ← 最先接收来料
ARRAY[2] = L2 (第2层)
ARRAY[3] = L3 (第3层)
ARRAY[4] = L4 (第4层 / 顶层)  ← 最后接收来料

物理布局示意:
     ┌─────────┐  L4 (顶层)  ARRAY[4]
     │  Layer4 │
     ├─────────┤  L3         ARRAY[3]
     │  Layer3 │
     ├─────────┤  L2         ARRAY[2]
     │  Layer2 │
     ├─────────┤  L1 (底层)  ARRAY[1]
     │  Layer1 │
     └─────────┘
        ↑ 来料方向(从下方进入)
```

---

## 9. 默认常量与参数回退机制

### 9.1 默认常量定义

| 常量名 | 默认值 | 用途 | 可调整性 |
|--------|:------:|:-----|:-------:|
| `T_DEF_推料伸出超时` | 5000ms (5s) | 步骤2超时保护 | ✅ |
| `T_DEF_输送带正转超时` | 10000ms (10s) | 步骤3超时保护 | ✅ |
| `T_DEF_产品到位检测超时` | 5000ms (5s) | 步骤4超时保护 | ✅ |
| `T_DEF_推料缩回超时` | 5000ms (5s) | 步骤6超时保护 | ✅ |
| `T_DEF_放料完成脉冲宽度` | 200ms | 步骤8脉冲持续时间 | ✅ |
| `T_DEF_输送带停止延时` | 100ms | 步骤5惯性停止延时 | ⚠️ 一般不改 |

### 9.2 回退机制说明

当外部未连接某些参数时，FB会使用上述默认常量作为回退值，确保功能不会因缺少参数而异常。

---

## 10. 相关文档链接

### 10.1 本FB文档体系

| 文档类型 | 文档名称 | 说明 |
|---------|---------|:-----|
| **详细设计说明书 (DSN)** | 本文档 | 两级架构、9步状态机、容器分发收集机制 |
| **接口文档 (IFC)** | [接口文档_IFC-FB1001-Conveyor4Layer-V4.0.0.md](./接口文档_IFC-FB1001-Conveyor4Layer-V4.0.0.md) | 单层24输入/10输出 + 容器公共+分层接口定义 |
| **使用说明 (UM)** | [使用说明_UM-FB1001-Conveyor4Layer-V4.0.0.md](./使用说明_UM-FB1001-Conveyor4Layer-V4.0.0.md) | ST调用示例(容器+单层)、IO地址映射表、多层联动测试 |
| **变更记录 (CHG)** | [变更记录_CHG-FB1001-Conveyor4Layer-V4.0.0.md](./变更记录_CHG-FB1001-Conveyor4Layer-V4.0.0.md) | 版本历史、变更台帐、FB重命名记录 |

### 10.2 关联FB文档

| FB名称 | 文档路径 | 关系说明 |
|--------|:---------|:--------|
| FB_1003_PickPlace (取放料机构) | `../取放料机构/` | 下游设备，接收o_Lx_放料完成[1..4]信号 |
| FB_2001_CommonAlarm (公共报警) | `../公共服务/` | 接收本FB的报警代码(101~199段) |

### 10.3 项目级文档

| 文档名称 | 路径 | 说明 |
|---------|:-----|:-----|
| PLC程序设计总文档 | `../../程序文档/016_DJ-2026-005_PLC程序设计总文档_PLC-V4.0.0.md` | 整体架构说明 |
| 主控详细设计说明书 | `../详细设计说明书_DSN-DJ-2026-005-V4.0.0.md` | 主控程序及IO映射设计 |
| PLC变量定义文档 | `../PLC变量定义文档_VAR-DJ-2026-005-V4.0.0.md` | 全局变量定义 |
| IO分配表 | `../../程序文档/015_DJ-2026-005_IO分配表_IO-V3.0.0.md` | 物理IO地址(DI3/DI4模块 + DO1/DO2模块) |
| 报警码定义 | `../公共服务/报警码定义_ALM-DJ-2026-005-V4.0.0.md` | 全局报警码体系 |

---

## 11. 附录

### 11.1 内部变量说明 (VAR)

#### FB_1002 SingleLayer 内部变量

| 变量名 | 类型 | 用途 |
|--------|:----:|:-----|
| `s_i步序` | INT | 当前状态机步序 (0~8, 99=故障) |
| `s_b初始化中` | BOOL | 初始化进行中标志 |
| `s_b初始化完成` | BOOL | 初始化完成标志 |
| `s_t*` 系列 | TON | 各步骤的超时定时器(5个) |
| `s_t放料完成脉冲` | TON | 步骤8的脉冲定时器 |

#### FB_1001 Conveyor4Layer 内部变量

| 变量名 | 类型 | 用途 |
|--------|:----:|:-----|
| `fbLayer1` ~ `fbLayer4` | FB_1002 | 4个单层FB实例 |

### 11.2 特殊设计决策记录

| 决策ID | 决策内容 | 原因 | 替代方案(未采用) |
|:------:|:---------|:-----|:---------------|
| D001 | 采用两级FB而非单一大FB | 降低复杂度，提高可维护性 | 单一FB处理4层(过于复杂,难维护) |
| D002 | 容器FB不含业务逻辑 | 职责单一，便于测试 | 容器也包含部分逻辑(增加耦合度) |
| D003 | 4层完全独立运行 | 避免互相干扰，简化调试 | 4层联动状态机(过于复杂,难以扩展) |
| D004 | 使用ARRAY[1..4]接口 | 统一对外接口，隐藏内部结构 | 每层独立接口(增加主控复杂度) |

---

**文档版本**: V4.0.0
**最后更新**: 2026-04-24
**下次审查日期**: [待定]
